import os
from dotenv import load_dotenv

load_dotenv()

class Settings:
    OPENAI_API_KEY: str = os.getenv("OPENAI_API_KEY", "")
    GPT_MODEL: str = os.getenv("GPT_MODEL", "gpt-3.5-turbo")
    GPT_TEMPERATURE: float = 0.0
    MAX_TOKENS: int = 500
    MAX_ANSWER_LENGTH: int = 5000
    MIN_ANSWER_LENGTH: int = 3
    APP_TITLE: str = "GPT Answer Evaluation API"
    APP_VERSION: str = "1.0.0"

settings = Settings()
